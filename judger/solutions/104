#include<iostream> 

using namespace std;

int main(){
	ios::sync_with_stdio(false);
	cin.tie(0);
	int n;
	cin>>n;
  int a=0,b=1;
  for(int i=0;i<n;i++){
    b=b%1000000007+a%1000000007 ;
    a=(b-a)%1000000007 ;
  }

  cout<<a;
	return 0;
}