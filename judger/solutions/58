def e(M, N):
	dp = [[0] * M for i in range(N)]
	dp[0][0] = 1
	for i in range(N):
		dp[i][0] = 1
		for j in range(1, M):
			dp[i][j] = dp[i][j - 1] + (dp[i - 1][j] if i != 0 else 0)
	return (dp[N - 1][M - 1])
print(e(int(input()), int(input())))